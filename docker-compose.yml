version: "3.9"

services:
  mysql:
    image: mysql:8.4
    container_name: mysql-shopdev
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root1234
      MYSQL_DATABASE: shopdev
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: